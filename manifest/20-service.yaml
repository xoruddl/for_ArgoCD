apiVersion: v1
kind: Service
metadata:
  name: bgd
  namespace: bgd
spec:
  type: NodePort
  selector:
    app: bgd
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      nodePort: 31080
